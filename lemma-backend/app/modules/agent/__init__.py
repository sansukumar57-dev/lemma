"""Unified agent module.

This module is intentionally independent from the deprecated v1 agent executor
and the older assistant runtime. It may depend on shared infrastructure and
framework libraries, but its domain, harness, and storage contracts are owned
here.
"""

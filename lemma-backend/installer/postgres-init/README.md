This directory is mounted into the local Postgres container.

It is intentionally empty for now; the dev orchestrator (scripts/dev.py)
creates the local databases and extensions after Postgres is ready.

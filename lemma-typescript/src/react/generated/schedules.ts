// GENERATED by scripts/generate_l2.mjs from x-lemma metadata — do not edit by hand.
// Resource: schedule. Regenerate with: npm run generate:l2
//
// Mechanical TanStack-Query hooks over the schedules facade. Query keys and the mutation
// invalidation graph are derived from the backend's x-lemma metadata, so a mutation
// refreshes exactly the lists/entities it should — no manual refetch. Types are pulled
// from the real facade via Parameters<>, so the compiler verifies every call.
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import type { LemmaClient } from "../../client.js";
import { resolvePodClient, resolvePodId, type GeneratedQueryOptions } from "../utils.js";

export function useScheduleCreate(client: LemmaClient, podId?: string) {
  const queryClient = useQueryClient();
  const scoped = resolvePodClient(client, podId);
  const pid = resolvePodId(client, podId);
  return useMutation({
    mutationFn: (vars: { payload: Parameters<LemmaClient["schedules"]["create"]>[0] }) => scoped.schedules.create(vars.payload),
    onSuccess: () => {
      void queryClient.invalidateQueries({ queryKey: ["schedule", pid] });
    },
  });
}

export function useScheduleDelete(client: LemmaClient, podId?: string) {
  const queryClient = useQueryClient();
  const scoped = resolvePodClient(client, podId);
  const pid = resolvePodId(client, podId);
  return useMutation({
    mutationFn: (vars: { scheduleId: Parameters<LemmaClient["schedules"]["delete"]>[0] }) => scoped.schedules.delete(vars.scheduleId),
    onSuccess: () => {
      void queryClient.invalidateQueries({ queryKey: ["schedule", pid] });
    },
  });
}

export function useScheduleGet(
  client: LemmaClient,
  podId: string | undefined,
  scheduleId: Parameters<LemmaClient["schedules"]["get"]>[0],
  queryOptions?: GeneratedQueryOptions,
) {
  const scoped = resolvePodClient(client, podId);
  const pid = resolvePodId(client, podId);
  return useQuery({
    queryKey: ["schedule", pid, "get", scheduleId],
    queryFn: () => scoped.schedules.get(scheduleId),
    ...queryOptions,
  });
}

export function useScheduleList(
  client: LemmaClient,
  podId: string | undefined,
  options?: Parameters<LemmaClient["schedules"]["list"]>[0],
  queryOptions?: GeneratedQueryOptions,
) {
  const scoped = resolvePodClient(client, podId);
  const pid = resolvePodId(client, podId);
  return useQuery({
    queryKey: ["schedule", pid, "list", options ?? null],
    queryFn: () => scoped.schedules.list(options),
    ...queryOptions,
  });
}

export function useScheduleUpdate(client: LemmaClient, podId?: string) {
  const queryClient = useQueryClient();
  const scoped = resolvePodClient(client, podId);
  const pid = resolvePodId(client, podId);
  return useMutation({
    mutationFn: (vars: { scheduleId: Parameters<LemmaClient["schedules"]["update"]>[0]; payload: Parameters<LemmaClient["schedules"]["update"]>[1] }) => scoped.schedules.update(vars.scheduleId, vars.payload),
    onSuccess: () => {
      void queryClient.invalidateQueries({ queryKey: ["schedule", pid] });
    },
  });
}

// GENERATED by scripts/generate_l2.mjs from x-lemma metadata — do not edit by hand.
// Resource: record. Regenerate with: npm run generate:l2
//
// Mechanical TanStack-Query hooks over the records facade. Query keys and the mutation
// invalidation graph are derived from the backend's x-lemma metadata, so a mutation
// refreshes exactly the lists/entities it should — no manual refetch. Types are pulled
// from the real facade via Parameters<>, so the compiler verifies every call.
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import type { LemmaClient } from "../../client.js";
import { resolvePodClient, resolvePodId, type GeneratedQueryOptions } from "../utils.js";

export function useRecordBulkCreate(client: LemmaClient, podId?: string) {
  const queryClient = useQueryClient();
  const scoped = resolvePodClient(client, podId);
  const pid = resolvePodId(client, podId);
  return useMutation({
    mutationFn: (vars: { tableName: Parameters<LemmaClient["records"]["bulk"]["create"]>[0]; payload: Parameters<LemmaClient["records"]["bulk"]["create"]>[1] }) => scoped.records.bulk.create(vars.tableName, vars.payload),
    onSuccess: () => {
      void queryClient.invalidateQueries({ queryKey: ["record", pid] });
    },
  });
}

export function useRecordBulkDelete(client: LemmaClient, podId?: string) {
  const queryClient = useQueryClient();
  const scoped = resolvePodClient(client, podId);
  const pid = resolvePodId(client, podId);
  return useMutation({
    mutationFn: (vars: { tableName: Parameters<LemmaClient["records"]["bulk"]["delete"]>[0]; payload: Parameters<LemmaClient["records"]["bulk"]["delete"]>[1] }) => scoped.records.bulk.delete(vars.tableName, vars.payload),
    onSuccess: () => {
      void queryClient.invalidateQueries({ queryKey: ["record", pid] });
    },
  });
}

export function useRecordBulkUpdate(client: LemmaClient, podId?: string) {
  const queryClient = useQueryClient();
  const scoped = resolvePodClient(client, podId);
  const pid = resolvePodId(client, podId);
  return useMutation({
    mutationFn: (vars: { tableName: Parameters<LemmaClient["records"]["bulk"]["update"]>[0]; payload: Parameters<LemmaClient["records"]["bulk"]["update"]>[1] }) => scoped.records.bulk.update(vars.tableName, vars.payload),
    onSuccess: () => {
      void queryClient.invalidateQueries({ queryKey: ["record", pid] });
    },
  });
}

export function useRecordCreate(client: LemmaClient, podId?: string) {
  const queryClient = useQueryClient();
  const scoped = resolvePodClient(client, podId);
  const pid = resolvePodId(client, podId);
  return useMutation({
    mutationFn: (vars: { tableName: Parameters<LemmaClient["records"]["create"]>[0]; payload: Parameters<LemmaClient["records"]["create"]>[1] }) => scoped.records.create(vars.tableName, vars.payload),
    onSuccess: () => {
      void queryClient.invalidateQueries({ queryKey: ["record", pid] });
    },
  });
}

export function useRecordDelete(client: LemmaClient, podId?: string) {
  const queryClient = useQueryClient();
  const scoped = resolvePodClient(client, podId);
  const pid = resolvePodId(client, podId);
  return useMutation({
    mutationFn: (vars: { tableName: Parameters<LemmaClient["records"]["delete"]>[0]; recordId: Parameters<LemmaClient["records"]["delete"]>[1] }) => scoped.records.delete(vars.tableName, vars.recordId),
    onSuccess: () => {
      void queryClient.invalidateQueries({ queryKey: ["record", pid] });
    },
  });
}

export function useRecordGet(
  client: LemmaClient,
  podId: string | undefined,
  tableName: Parameters<LemmaClient["records"]["get"]>[0],
  recordId: Parameters<LemmaClient["records"]["get"]>[1],
  queryOptions?: GeneratedQueryOptions,
) {
  const scoped = resolvePodClient(client, podId);
  const pid = resolvePodId(client, podId);
  return useQuery({
    queryKey: ["record", pid, "get", tableName, recordId],
    queryFn: () => scoped.records.get(tableName, recordId),
    ...queryOptions,
  });
}

export function useRecordList(
  client: LemmaClient,
  podId: string | undefined,
  tableName: Parameters<LemmaClient["records"]["list"]>[0],
  options?: Parameters<LemmaClient["records"]["list"]>[1],
  queryOptions?: GeneratedQueryOptions,
) {
  const scoped = resolvePodClient(client, podId);
  const pid = resolvePodId(client, podId);
  return useQuery({
    queryKey: ["record", pid, "list", tableName, options ?? null],
    queryFn: () => scoped.records.list(tableName, options),
    ...queryOptions,
  });
}

export function useRecordUpdate(client: LemmaClient, podId?: string) {
  const queryClient = useQueryClient();
  const scoped = resolvePodClient(client, podId);
  const pid = resolvePodId(client, podId);
  return useMutation({
    mutationFn: (vars: { tableName: Parameters<LemmaClient["records"]["update"]>[0]; recordId: Parameters<LemmaClient["records"]["update"]>[1]; payload: Parameters<LemmaClient["records"]["update"]>[2] }) => scoped.records.update(vars.tableName, vars.recordId, vars.payload),
    onSuccess: () => {
      void queryClient.invalidateQueries({ queryKey: ["record", pid] });
    },
  });
}

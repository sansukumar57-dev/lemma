// GENERATED by scripts/generate_l2.mjs from x-lemma metadata — do not edit by hand.
// Resource: function. Regenerate with: npm run generate:l2
//
// Mechanical TanStack-Query hooks over the functions facade. Query keys and the mutation
// invalidation graph are derived from the backend's x-lemma metadata, so a mutation
// refreshes exactly the lists/entities it should — no manual refetch. Types are pulled
// from the real facade via Parameters<>, so the compiler verifies every call.
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import type { LemmaClient } from "../../client.js";
import { resolvePodClient, resolvePodId, type GeneratedQueryOptions } from "../utils.js";

export function useFunctionCreate(client: LemmaClient, podId?: string) {
  const queryClient = useQueryClient();
  const scoped = resolvePodClient(client, podId);
  const pid = resolvePodId(client, podId);
  return useMutation({
    mutationFn: (vars: { payload: Parameters<LemmaClient["functions"]["create"]>[0] }) => scoped.functions.create(vars.payload),
    onSuccess: () => {
      void queryClient.invalidateQueries({ queryKey: ["function", pid] });
    },
  });
}

export function useFunctionDelete(client: LemmaClient, podId?: string) {
  const queryClient = useQueryClient();
  const scoped = resolvePodClient(client, podId);
  const pid = resolvePodId(client, podId);
  return useMutation({
    mutationFn: (vars: { functionName: Parameters<LemmaClient["functions"]["delete"]>[0] }) => scoped.functions.delete(vars.functionName),
    onSuccess: () => {
      void queryClient.invalidateQueries({ queryKey: ["function", pid] });
    },
  });
}

export function useFunctionGet(
  client: LemmaClient,
  podId: string | undefined,
  functionName: Parameters<LemmaClient["functions"]["get"]>[0],
  queryOptions?: GeneratedQueryOptions,
) {
  const scoped = resolvePodClient(client, podId);
  const pid = resolvePodId(client, podId);
  return useQuery({
    queryKey: ["function", pid, "get", functionName],
    queryFn: () => scoped.functions.get(functionName),
    ...queryOptions,
  });
}

export function useFunctionList(
  client: LemmaClient,
  podId: string | undefined,
  options?: Parameters<LemmaClient["functions"]["list"]>[0],
  queryOptions?: GeneratedQueryOptions,
) {
  const scoped = resolvePodClient(client, podId);
  const pid = resolvePodId(client, podId);
  return useQuery({
    queryKey: ["function", pid, "list", options ?? null],
    queryFn: () => scoped.functions.list(options),
    ...queryOptions,
  });
}

export function useFunctionUpdate(client: LemmaClient, podId?: string) {
  const queryClient = useQueryClient();
  const scoped = resolvePodClient(client, podId);
  const pid = resolvePodId(client, podId);
  return useMutation({
    mutationFn: (vars: { functionName: Parameters<LemmaClient["functions"]["update"]>[0]; payload: Parameters<LemmaClient["functions"]["update"]>[1] }) => scoped.functions.update(vars.functionName, vars.payload),
    onSuccess: () => {
      void queryClient.invalidateQueries({ queryKey: ["function", pid] });
    },
  });
}

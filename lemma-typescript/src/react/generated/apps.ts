// GENERATED by scripts/generate_l2.mjs from x-lemma metadata — do not edit by hand.
// Resource: app. Regenerate with: npm run generate:l2
//
// Mechanical TanStack-Query hooks over the apps facade. Query keys and the mutation
// invalidation graph are derived from the backend's x-lemma metadata, so a mutation
// refreshes exactly the lists/entities it should — no manual refetch. Types are pulled
// from the real facade via Parameters<>, so the compiler verifies every call.
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import type { LemmaClient } from "../../client.js";
import { resolvePodClient, resolvePodId, type GeneratedQueryOptions } from "../utils.js";

export function useAppCreate(client: LemmaClient, podId?: string) {
  const queryClient = useQueryClient();
  const scoped = resolvePodClient(client, podId);
  const pid = resolvePodId(client, podId);
  return useMutation({
    mutationFn: (vars: { payload: Parameters<LemmaClient["apps"]["create"]>[0] }) => scoped.apps.create(vars.payload),
    onSuccess: () => {
      void queryClient.invalidateQueries({ queryKey: ["app", pid] });
    },
  });
}

export function useAppDelete(client: LemmaClient, podId?: string) {
  const queryClient = useQueryClient();
  const scoped = resolvePodClient(client, podId);
  const pid = resolvePodId(client, podId);
  return useMutation({
    mutationFn: (vars: { appName: Parameters<LemmaClient["apps"]["delete"]>[0] }) => scoped.apps.delete(vars.appName),
    onSuccess: () => {
      void queryClient.invalidateQueries({ queryKey: ["app", pid] });
    },
  });
}

export function useAppGet(
  client: LemmaClient,
  podId: string | undefined,
  appName: Parameters<LemmaClient["apps"]["get"]>[0],
  queryOptions?: GeneratedQueryOptions,
) {
  const scoped = resolvePodClient(client, podId);
  const pid = resolvePodId(client, podId);
  return useQuery({
    queryKey: ["app", pid, "get", appName],
    queryFn: () => scoped.apps.get(appName),
    ...queryOptions,
  });
}

export function useAppList(
  client: LemmaClient,
  podId: string | undefined,
  options?: Parameters<LemmaClient["apps"]["list"]>[0],
  queryOptions?: GeneratedQueryOptions,
) {
  const scoped = resolvePodClient(client, podId);
  const pid = resolvePodId(client, podId);
  return useQuery({
    queryKey: ["app", pid, "list", options ?? null],
    queryFn: () => scoped.apps.list(options),
    ...queryOptions,
  });
}

export function useAppUpdate(client: LemmaClient, podId?: string) {
  const queryClient = useQueryClient();
  const scoped = resolvePodClient(client, podId);
  const pid = resolvePodId(client, podId);
  return useMutation({
    mutationFn: (vars: { appName: Parameters<LemmaClient["apps"]["update"]>[0]; payload: Parameters<LemmaClient["apps"]["update"]>[1] }) => scoped.apps.update(vars.appName, vars.payload),
    onSuccess: () => {
      void queryClient.invalidateQueries({ queryKey: ["app", pid] });
    },
  });
}

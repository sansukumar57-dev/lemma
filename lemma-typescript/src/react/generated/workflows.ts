// GENERATED by scripts/generate_l2.mjs from x-lemma metadata — do not edit by hand.
// Resource: workflow. Regenerate with: npm run generate:l2
//
// Mechanical TanStack-Query hooks over the workflows facade. Query keys and the mutation
// invalidation graph are derived from the backend's x-lemma metadata, so a mutation
// refreshes exactly the lists/entities it should — no manual refetch. Types are pulled
// from the real facade via Parameters<>, so the compiler verifies every call.
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import type { LemmaClient } from "../../client.js";
import { resolvePodClient, resolvePodId, type GeneratedQueryOptions } from "../utils.js";

export function useWorkflowCreate(client: LemmaClient, podId?: string) {
  const queryClient = useQueryClient();
  const scoped = resolvePodClient(client, podId);
  const pid = resolvePodId(client, podId);
  return useMutation({
    mutationFn: (vars: { payload: Parameters<LemmaClient["workflows"]["create"]>[0] }) => scoped.workflows.create(vars.payload),
    onSuccess: () => {
      void queryClient.invalidateQueries({ queryKey: ["workflow", pid] });
    },
  });
}

export function useWorkflowDelete(client: LemmaClient, podId?: string) {
  const queryClient = useQueryClient();
  const scoped = resolvePodClient(client, podId);
  const pid = resolvePodId(client, podId);
  return useMutation({
    mutationFn: (vars: { workflowName: Parameters<LemmaClient["workflows"]["delete"]>[0] }) => scoped.workflows.delete(vars.workflowName),
    onSuccess: () => {
      void queryClient.invalidateQueries({ queryKey: ["workflow", pid] });
    },
  });
}

export function useWorkflowGet(
  client: LemmaClient,
  podId: string | undefined,
  workflowName: Parameters<LemmaClient["workflows"]["get"]>[0],
  queryOptions?: GeneratedQueryOptions,
) {
  const scoped = resolvePodClient(client, podId);
  const pid = resolvePodId(client, podId);
  return useQuery({
    queryKey: ["workflow", pid, "get", workflowName],
    queryFn: () => scoped.workflows.get(workflowName),
    ...queryOptions,
  });
}

export function useWorkflowList(
  client: LemmaClient,
  podId: string | undefined,
  options?: Parameters<LemmaClient["workflows"]["list"]>[0],
  queryOptions?: GeneratedQueryOptions,
) {
  const scoped = resolvePodClient(client, podId);
  const pid = resolvePodId(client, podId);
  return useQuery({
    queryKey: ["workflow", pid, "list", options ?? null],
    queryFn: () => scoped.workflows.list(options),
    ...queryOptions,
  });
}

export function useWorkflowUpdate(client: LemmaClient, podId?: string) {
  const queryClient = useQueryClient();
  const scoped = resolvePodClient(client, podId);
  const pid = resolvePodId(client, podId);
  return useMutation({
    mutationFn: (vars: { workflowName: Parameters<LemmaClient["workflows"]["update"]>[0]; payload: Parameters<LemmaClient["workflows"]["update"]>[1] }) => scoped.workflows.update(vars.workflowName, vars.payload),
    onSuccess: () => {
      void queryClient.invalidateQueries({ queryKey: ["workflow", pid] });
    },
  });
}

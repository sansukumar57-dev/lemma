// GENERATED by scripts/generate_l2.mjs from x-lemma metadata — do not edit by hand.
// Resource: table. Regenerate with: npm run generate:l2
//
// Mechanical TanStack-Query hooks over the tables facade. Query keys and the mutation
// invalidation graph are derived from the backend's x-lemma metadata, so a mutation
// refreshes exactly the lists/entities it should — no manual refetch. Types are pulled
// from the real facade via Parameters<>, so the compiler verifies every call.
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import type { LemmaClient } from "../../client.js";
import { resolvePodClient, resolvePodId, type GeneratedQueryOptions } from "../utils.js";

export function useTableCreate(client: LemmaClient, podId?: string) {
  const queryClient = useQueryClient();
  const scoped = resolvePodClient(client, podId);
  const pid = resolvePodId(client, podId);
  return useMutation({
    mutationFn: (vars: { payload: Parameters<LemmaClient["tables"]["create"]>[0] }) => scoped.tables.create(vars.payload),
    onSuccess: () => {
      void queryClient.invalidateQueries({ queryKey: ["table", pid] });
    },
  });
}

export function useTableDelete(client: LemmaClient, podId?: string) {
  const queryClient = useQueryClient();
  const scoped = resolvePodClient(client, podId);
  const pid = resolvePodId(client, podId);
  return useMutation({
    mutationFn: (vars: { tableName: Parameters<LemmaClient["tables"]["delete"]>[0] }) => scoped.tables.delete(vars.tableName),
    onSuccess: () => {
      void queryClient.invalidateQueries({ queryKey: ["table", pid] });
    },
  });
}

export function useTableGet(
  client: LemmaClient,
  podId: string | undefined,
  tableName: Parameters<LemmaClient["tables"]["get"]>[0],
  queryOptions?: GeneratedQueryOptions,
) {
  const scoped = resolvePodClient(client, podId);
  const pid = resolvePodId(client, podId);
  return useQuery({
    queryKey: ["table", pid, "get", tableName],
    queryFn: () => scoped.tables.get(tableName),
    ...queryOptions,
  });
}

export function useTableList(
  client: LemmaClient,
  podId: string | undefined,
  options?: Parameters<LemmaClient["tables"]["list"]>[0],
  queryOptions?: GeneratedQueryOptions,
) {
  const scoped = resolvePodClient(client, podId);
  const pid = resolvePodId(client, podId);
  return useQuery({
    queryKey: ["table", pid, "list", options ?? null],
    queryFn: () => scoped.tables.list(options),
    ...queryOptions,
  });
}

export function useTableUpdate(client: LemmaClient, podId?: string) {
  const queryClient = useQueryClient();
  const scoped = resolvePodClient(client, podId);
  const pid = resolvePodId(client, podId);
  return useMutation({
    mutationFn: (vars: { tableName: Parameters<LemmaClient["tables"]["update"]>[0]; payload: Parameters<LemmaClient["tables"]["update"]>[1] }) => scoped.tables.update(vars.tableName, vars.payload),
    onSuccess: () => {
      void queryClient.invalidateQueries({ queryKey: ["table", pid] });
    },
  });
}

// GENERATED by scripts/generate_l2.mjs from x-lemma metadata — do not edit by hand.
// Resource: agent. Regenerate with: npm run generate:l2
//
// Mechanical TanStack-Query hooks over the agents facade. Query keys and the mutation
// invalidation graph are derived from the backend's x-lemma metadata, so a mutation
// refreshes exactly the lists/entities it should — no manual refetch. Types are pulled
// from the real facade via Parameters<>, so the compiler verifies every call.
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import type { LemmaClient } from "../../client.js";
import { resolvePodClient, resolvePodId, type GeneratedQueryOptions } from "../utils.js";

export function useAgentCreate(client: LemmaClient, podId?: string) {
  const queryClient = useQueryClient();
  const scoped = resolvePodClient(client, podId);
  const pid = resolvePodId(client, podId);
  return useMutation({
    mutationFn: (vars: { payload: Parameters<LemmaClient["agents"]["create"]>[0] }) => scoped.agents.create(vars.payload),
    onSuccess: () => {
      void queryClient.invalidateQueries({ queryKey: ["agent", pid] });
    },
  });
}

export function useAgentDelete(client: LemmaClient, podId?: string) {
  const queryClient = useQueryClient();
  const scoped = resolvePodClient(client, podId);
  const pid = resolvePodId(client, podId);
  return useMutation({
    mutationFn: (vars: { agentName: Parameters<LemmaClient["agents"]["delete"]>[0] }) => scoped.agents.delete(vars.agentName),
    onSuccess: () => {
      void queryClient.invalidateQueries({ queryKey: ["agent", pid] });
    },
  });
}

export function useAgentGet(
  client: LemmaClient,
  podId: string | undefined,
  agentName: Parameters<LemmaClient["agents"]["get"]>[0],
  queryOptions?: GeneratedQueryOptions,
) {
  const scoped = resolvePodClient(client, podId);
  const pid = resolvePodId(client, podId);
  return useQuery({
    queryKey: ["agent", pid, "get", agentName],
    queryFn: () => scoped.agents.get(agentName),
    ...queryOptions,
  });
}

export function useAgentList(
  client: LemmaClient,
  podId: string | undefined,
  options?: Parameters<LemmaClient["agents"]["list"]>[0],
  queryOptions?: GeneratedQueryOptions,
) {
  const scoped = resolvePodClient(client, podId);
  const pid = resolvePodId(client, podId);
  return useQuery({
    queryKey: ["agent", pid, "list", options ?? null],
    queryFn: () => scoped.agents.list(options),
    ...queryOptions,
  });
}

export function useAgentUpdate(client: LemmaClient, podId?: string) {
  const queryClient = useQueryClient();
  const scoped = resolvePodClient(client, podId);
  const pid = resolvePodId(client, podId);
  return useMutation({
    mutationFn: (vars: { agentName: Parameters<LemmaClient["agents"]["update"]>[0]; payload: Parameters<LemmaClient["agents"]["update"]>[1] }) => scoped.agents.update(vars.agentName, vars.payload),
    onSuccess: () => {
      void queryClient.invalidateQueries({ queryKey: ["agent", pid] });
    },
  });
}

services:
  lemma-frontend:
    image: lemma-frontend:local
    build:
      context: ..
      dockerfile: lemma-frontend/Dockerfile
    env_file:
      - .env
    ports:
      - "8080:8080"
    restart: unless-stopped

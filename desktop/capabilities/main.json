{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Splash window talks to the shell over IPC",
  "windows": ["main"],
  "permissions": ["core:default"]
}
